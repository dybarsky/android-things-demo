buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.3.0'
    }
}

apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion '25.0.2'

    defaultConfig {
        applicationId "com.tooploox.iot.example"
        minSdkVersion 24
        targetSdkVersion 25
        versionCode 1
        versionName "0.1"
    }
}

repositories {
    jcenter()
}

dependencies {
    provided 'com.google.android.things:androidthings:0.2-devpreview'
}
